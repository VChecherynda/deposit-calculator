version: '3'

services:
  nextjs:
    build:
      context: .
      target: runner
    restart: always
    networks:
      - app-network

  nginx:
    build:
      context: .
      target: nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - nextjs
    networks:
      - app-network

networks:
  app-network:
    driver: bridge